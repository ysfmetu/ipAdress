spring:
    datasource:
      username: root
      password: root
      url: jdbc:mysql://localhost:3306/sunucu
    jpa:
      hibernate:
        ddl-auto: update
      properties:
        hibernate:
          dialect:org.hibernate.dialect.MySQL8Dialect8Dialect
          format_sql:true
      show-sql: true
